## 1. Фаза 0 — Аудит и подготовка (без изменений кода)

- [x] 1.1 Провести инвентаризацию Blade-шаблонов (`resources/views/**/*.blade.php`) и используемых CSS-классов (grep по классам из `style.scss` в шаблонах); составить список используемых/неиспользуемых (мёртвых) классов в `docs/design-audit-inventory.md`
- [x] 1.2 Сделать скриншоты текущего вида страниц index, article, contact и ошибки 404 на ширинах 375/768/1280px и сохранить в `docs/screenshots/before/` для регрессии
- [x] 1.3 Снять замеры Lighthouse (Performance, Accessibility, Best Practices, SEO) на главной и странице статьи до изменений и зафиксировать значения в `docs/lighthouse-before.md`

## 2. Дизайн-токены

- [x] 2.1 Создать `resources/sass/_tokens.scss` с CSS custom properties в `:root` (цвет, типографика, spacing по шкале 4/8/12/16/24/32/48/64, радиусы, тени), значения — из текущей палитры; подключить партиал в `style.scss` и проверить `npm run build` без ошибок
- [x] 2.2 Перенести цветовые значения из `resources/sass/_variables.scss` в токены (с комментарием legacy-соответствия), сохранив сам файл `_variables.scss`; проверить, что сборка не содержит ошибок undefined variable и каждый цвет `_variables.scss` представлен токеном в `:root`

## 3. Модульная структура SCSS

- [x] 3.1 Создать `resources/sass/_base.scss`: перенести импорт `../css/normalize.css` и базовые стили (html/body, ссылки, списки, код/pre), подключить в оркестратор и проверить `npm run build`
- [x] 3.2 Создать `resources/sass/_layout.scss`: перенести `.main_block`, `.wrap`, `.nav_wrap`, шапку, футер; исправить баг `max-width: 1170;` → `1170px` и заменить нерабочие `@media (max-widht: ...)` на корректную нотацию (или удалить с фиксацией); проверить `npm run build` и соответствие внешнего вида скриншотам «до»
- [x] 3.3 Создать `resources/sass/_components.scss`: перенести стили карточек, алертов, пагинации, форм, комментариев, тегов из `style.scss`; проверить `npm run build`
- [x] 3.4 Создать `resources/sass/_dark.scss` как пустой каркас с комментарием «Тёмная тема — Фаза 5», подключить в оркестратор и проверить `npm run build`
- [x] 3.5 Превратить `resources/sass/style.scss` в тонкий оркестратор: подключить партиалы через `@use` (`variables as *`, `tokens`, `base`, `layout`, `components`, `dark`), удалить дублирующиеся правила; проверить `npm run build` без ошибок и визуальную идентичность наследуемых страниц со скриншотами «до»

## 4. Базовая типографика

- [ ] 4.1 Задать системный шрифтовой стек через токен `--font-sans` (`system-ui, -apple-system, 'Segoe UI', Roboto, ...`), базовый `font-size: 16px` на html/body; удалить правило `@media screen and (max-width: 900px) { html, body { font-size: 12px; } }`; проверить в DevTools на viewport 320–900px, что вычисленный `font-size` основного текста ≥ 16px
- [ ] 4.2 Перевести заголовки h1–h6 на fluid-размеры через `clamp()`; проверить, что размер заголовков монотонно меняется при ресайзе окна между минимальным и максимальным значениями

## 5. Доступный viewport

- [ ] 5.1 Убрать `maximum-scale=1.0, user-scalable=0` из метатега viewport в `resources/views/layouts/base.blade.php`, оставив `width=device-width, initial-scale=1.0`; проверить, что метатег не содержит `maximum-scale`/`user-scalable` и пинч-зум работает на мобильном устройстве/эмуляторе

## 6. Интеграционная верификация

- [ ] 6.1 Выполнить `npm run build` и `npm run dev` — обе команды завершаются без ошибок, имя выходного файла стилей не изменилось
- [ ] 6.2 Прогнать Lighthouse на главной и статье: отсутствуют предупреждения о запрете масштабирования (viewport allows zoom); сравнить результаты с замерами из `docs/lighthouse-before.md`
- [ ] 6.3 Сравнить визуально index/article/contact/404 на 375/768/1280px со скриншотами `docs/screenshots/before/`: допустимы различия только в типографике и возможности зума, макет и контент не должны ломаться
- [ ] 6.4 Открыть админку Orchid (`/admin`) и убедиться, что её стили и функциональность не затронуты изменениями токенов и реорганизацией SCSS
